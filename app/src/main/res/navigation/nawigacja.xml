<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nawigacja">

    <fragment
        android:id="@+id/classList"
        android:name="com.example.teacherassistant.ClassList"
        android:label="fragment_class_list"
        tools:layout="@layout/fragment_class_list" >
        <action
            android:id="@+id/action_classList_to_addClass2"
            app:destination="@id/addClass" />
    </fragment>
    <fragment
        android:id="@+id/addClass"
        android:name="com.example.teacherassistant.addClass"
        android:label="addClass" >
        <action
            android:id="@+id/action_addClass_to_classList"
            app:destination="@id/classList" />
    </fragment>
</navigation>